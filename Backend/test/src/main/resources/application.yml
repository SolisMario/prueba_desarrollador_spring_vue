server:
  port: 9000
spring:
  application:
      name: spring-boot-rest-api
  datasource:
    url: jdbc:sqlserver://localhost:1430;databaseName=test;encrypt=true;trustServerCertificate=true
    username: sa
    password: Rootpass#1
    driver-className: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
      show-sql: false
    open-in-view: true
    database-platform: org.hibernate.dialect.SQLServer2012Dialect
